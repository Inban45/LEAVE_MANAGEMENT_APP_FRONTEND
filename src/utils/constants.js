// Roles
export const ROLES = {
  ADMIN: "ADMIN",
  MANAGER: "MANAGER",
  EMPLOYEE: "EMPLOYEE"
};

// Leave statuses
export const LEAVE_STATUS = {
  PENDING: "PENDING",
  APPROVED: "APPROVED",
  REJECTED: "REJECTED"
};

// Notification types
export const NOTIFICATION_TYPES = {
  INFO: "INFO",
  ALERT: "ALERT"
};

// Messages
export const MESSAGES = {
  LOGIN_SUCCESS: "Login successful",
  REGISTER_SUCCESS: "Registration successful"
};

// Direct exports for old imports (optional, for backward compatibility)
export const ROLE_ADMIN = ROLES.ADMIN;
export const ROLE_MANAGER = ROLES.MANAGER;
export const ROLE_EMPLOYEE = ROLES.EMPLOYEE;
